# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Database Configuration
DATABASE_PATH=./data

# Session Secret (REQUIRED)
# Generate a secure random secret with: openssl rand -hex 64
# This must be at least 64 characters. NEVER commit your actual secret to version control!
SESSION_SECRET=GENERATE_YOURS_USING_OPENSSL_RAND_HEX_64

# Production Mode
# Set to 'true' to enable secure cookies and HTTPS-only mode
PRODUCTION=false

# Frontend Origin (CORS)
# The origin of your frontend application
FRONTEND_ORIGIN=http://localhost:5173
